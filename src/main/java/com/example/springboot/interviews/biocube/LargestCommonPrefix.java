package com.example.springboot.interviews.biocube;
//select id,name ,max(salary) from Employee where salary<  ( select max(salary) from employee);
//
//select id,email,count(*) from Employee  group by email;
public class LargestCommonPrefix {

    public String largestCommonPrefix(String [] strArray){
        String longestCommonPrefix="";
            return longestCommonPrefix;
    }
}
